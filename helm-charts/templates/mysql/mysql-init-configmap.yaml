apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "application-template.fullname" . }}-mysql-init
  namespace: {{ .Values.deployment.namespace }}
  labels:
    {{- include "application-template.labels" . | nindent 4 }}
data:
  init.sql: |-
    DROP DATABASE IF EXISTS {{ .Values.mysql.database }};
    DROP USER IF EXISTS `$MYSQL_ADMIN_USER`@'%';
    DROP USER IF EXISTS `$MYSQL_USER`@'%';
    DROP USER IF EXISTS `{{ .Values.mysql.user }}`@`%`;
    CREATE DATABASE IF NOT EXISTS {{ .Values.mysql.database }} CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
    CREATE USER IF NOT EXISTS `$MYSQL_ADMIN_USER`@'%' IDENTIFIED BY '$MYSQL_ADMIN_PASSWORD';
    GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, REFERENCES, INDEX, ALTER, EXECUTE, CREATE VIEW, SHOW VIEW,
      CREATE ROUTINE, ALTER ROUTINE, EVENT, TRIGGER ON `{{ .Values.mysql.database }}`.* TO `$MYSQL_ADMIN_USER`@'%';
    CREATE USER IF NOT EXISTS `$MYSQL_USER`@'%' IDENTIFIED BY '$MYSQL_PASSWORD';
    GRANT SELECT, INSERT, UPDATE, DELETE, SHOW VIEW ON `{{ .Values.mysql.database }}`.* TO `{{ .Values.mysql.user }}`@`%`;
    FLUSH PRIVILEGES;